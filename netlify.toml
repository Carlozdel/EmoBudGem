
[build]
  publish = "."       
  functions = "netlify/functions" 

[dev]
publish = "."    
  functions = "netlify/functions"
  port = 8888     

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["googleapis"]